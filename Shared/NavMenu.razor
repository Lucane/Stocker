@*<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Stocker</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="notebooks">
                <span class="oi oi-laptop" aria-hidden="true"></span> Sülearvutid
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="connections">
                <span class="oi oi-link-intact" aria-hidden="true"></span> Hulgilaod
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
*@

<MudNavMenu>
    <MudNavLink Href="" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.Home">Home</MudNavLink>
    <MudNavLink Href="notebooks" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.List">Sülearvutid</MudNavLink>
    <MudNavLink Href="parts" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.Hardware">Varuosad</MudNavLink>
    <MudNavLink Href="connections" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.Link">Hulgilaod</MudNavLink>
</MudNavMenu>
